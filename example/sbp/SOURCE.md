# SOURCE

This is a generated deployment artifact. Do not edit by hand.

Canonical source: [ottojung/skrynia-apps](https://github.com/ottojung/skrynia-apps) commit `c4576ff1ef213d8e7e08c1861764a43847b58072`, app `source/serhii-birthday-guide`.

The `site/` directory contains the pre-built static output from that commit. The `Makefile` copies `site/` into `build/` so that Skrynia's `make build` target produces the expected directory.
